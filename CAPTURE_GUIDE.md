# 캡쳐/증거 수집 가이드

미션 제출 시 평가자가 GitHub Repository 링크만으로 모든 수행 결과를 확인할 수 있어야 한다.
모든 캡쳐와 로그는 README.md에 포함되거나 README에서 링크로 접근 가능해야 한다.

---

## 공통 규칙 (미션 전체 적용)

1. **캡쳐에는 명령어 입력 + 출력 결과가 반드시 함께 포함**되어야 한다.
2. **터미널 출력**은 코드블록(````bash`)으로 README에 기록한다.
3. **브라우저 접속 증거**는 주소창(포트 포함)과 응답 화면이 함께 보여야 한다.
4. **민감정보**(토큰, 비밀번호, 인증코드)는 마스킹 필수. 노출 시 즉시 제거 + 재발급.
5. README만 보고도 평가자가 동일 절차를 따라 재현할 수 있어야 한다.

---

## Week1 Mission1 필수 캡쳐 목록

### [📸필수] 브라우저 스크린샷
- 포트 매핑 접속: `localhost:8080` 브라우저 화면 (주소창 포트 포함)

### [📸필수] 전/후 비교 캡쳐
- 권한 변경: `chmod` 적용 전/후 `ls -la` 출력 비교
- 바인드 마운트: 호스트 파일 변경 전/후 브라우저 또는 curl 응답 비교
- Docker 볼륨: 컨테이너 삭제 전/후 데이터 유지 확인 (`cat /data/파일명`)

### [📸필수] 연동 증거
- GitHub + VSCode 연동 완료 화면 (저장소 메인 페이지)

### [코드블록] README에 출력 기록
- `pwd`, `ls -la`, `mkdir`, `cp`, `mv`, `rm` 실행 결과
- `docker --version`, `docker info` 출력
- `docker run hello-world` 출력
- ubuntu 컨테이너 내부 `ls`, `echo` 실행 결과
- `docker images`, `docker ps -a`, `docker logs <id>`, `docker stats` 출력
- Dockerfile 빌드 명령 + 출력 (`docker build -t my-web:1.0 .`)
- 볼륨 생성/연결/검증 명령 전체 시퀀스
- `git config --list` 출력

---

## 앞으로 미션별 추가 방법

각 미션 시작 시 이 파일 하단에 섹션 추가:

```
## Week2 Mission2 필수 캡쳐 목록
...
```

---

## 제출 전 체크리스트

- [ ] 모든 [📸필수] 항목 README에 첨부 완료
- [ ] 터미널 출력 코드블록으로 정리 완료
- [ ] 민감정보 마스킹 확인
- [ ] README 링크로 모든 증거 접근 가능 확인
